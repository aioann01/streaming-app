image: node:20

stages:
  - test

cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - node_modules/
    - .npm/

before_script:
  - npm ci --cache .npm --prefer-offline

unit_tests:
  stage: test
  script:
    - npm run test
  only:
    - merge_requests
    - main